# DAPPSNAP — Environment Variables
# Copy this file to .env and fill in your values.
# NEVER commit .env to git.

# Chrome CDP connection (same machine = localhost)
CDP_HOST=localhost
CDP_PORT=9222

# Default URL to record
DAPPSNAP_URL=https://glm.octant.app/
